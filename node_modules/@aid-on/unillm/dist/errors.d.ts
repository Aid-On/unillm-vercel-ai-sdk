/**
 * Error Types for LLM Provider
 *
 * Unified error handling across all providers (Groq, Gemini, Cloudflare)
 */
import type { ProviderType } from "./types.js";
/**
 * Error codes for categorization
 */
export type LLMErrorCode = "RATE_LIMIT" | "AUTH" | "TIMEOUT" | "INVALID_RESPONSE" | "NETWORK" | "SERVER_ERROR" | "VALIDATION" | "UNKNOWN";
/**
 * Main error class for LLM provider errors
 */
export declare class LLMProviderError extends Error {
    readonly code: LLMErrorCode;
    readonly provider: ProviderType | "unknown";
    readonly retryable: boolean;
    readonly statusCode?: number;
    readonly cause?: Error;
    constructor(options: {
        message: string;
        code: LLMErrorCode;
        provider?: ProviderType;
        statusCode?: number;
        retryable?: boolean;
        cause?: Error;
    });
    /**
     * Create a user-friendly error message
     */
    toUserMessage(): string;
}
/**
 * Determine if an error code is retryable by default
 */
export declare function isRetryableCode(code: LLMErrorCode): boolean;
/**
 * Parse an HTTP status code to an error code
 */
export declare function statusToErrorCode(status: number): LLMErrorCode;
/**
 * Wrap any error into an LLMProviderError
 */
export declare function wrapError(error: unknown, provider?: ProviderType, context?: string): LLMProviderError;
/**
 * Check if an error is an LLMProviderError with a specific code
 */
export declare function isLLMError(error: unknown, code?: LLMErrorCode): error is LLMProviderError;
/**
 * Check if an error is retryable
 */
export declare function isRetryable(error: unknown): boolean;
//# sourceMappingURL=errors.d.ts.map